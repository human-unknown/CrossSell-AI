# 后端 API 基地址（必须含 /api 后缀，后端路由均以 /api 为前缀）
# 本地开发: http://localhost:8000/api
# 公网隧道: https://xxx.ngrok.io/api  (ngrok http 8000)
# CloudStudio/生产: https://your-backend.com/api
VITE_API_BASE=http://localhost:8000/api

# 设为 true 可在后端不可用时降级为 mock 数据
VITE_USE_MOCK=false
